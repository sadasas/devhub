# Dev lokal: API langsung + vite proxy (/api + /ws → VITE_API_PROXY_TARGET).
VITE_API_URL=http://localhost:3000/api/v1
# VITE_API_PROXY_TARGET=http://localhost:3000
# Prod (dashboard Cloudflare, baked at build): VITE_API_URL=/api/v1
# (same-origin via Worker proxy devhub-admin — JANGAN absolut suga.run, ADR-051).
